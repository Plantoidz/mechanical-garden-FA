1. Create "confession" mode where a prompt is given to human and all plantoids address the question
1a. Figure out if the prompt clipping mode for confession should re-surface the human's answer, or use a generic min(5, n_plantoids)
2. Within event classes, create a function which can disambiguate arduino communication modes (USB, serial, etc... for Led lighting) ie: communicate_with_arduino()
3. Integrate Ben output on streaming, litellm etc

NEW VENV
sudo apt-get install portaudio19-dev
conda install -c conda-forge deepgram-sdk
pip install pyaudio
# conda install -c conda-forge pyaudio
pip install -r requirements.txt

//claude-3-opus-20240229

Agents should have a thread that reads a multiprocessing queue where the server populates it by registering the found ESP id in the queue,
the agent thread then checks if it matches its own agent id

https://docs.docker.com/engine/install/ubuntu/
https://docs.docker.com/compose/install/standalone/

docker exec -it mechanical-garden-fa-web-1 bash
sudo groupadd docker
sudo usermod -aG docker $USER
newgrp docker

docker run --rm -it --privileged=true --device=/dev/snd:/dev/snd mechanical-garden-fa-mg-1

docker-compose logs -f
web-1  | 2024-06-11 00:35:23,821 - INFO - Using default voice, both coqui_default_voice.json and coqui_default_voice.wav not found.
https://github.com/mviereck/x11docker/wiki/Container-sound:-ALSA-or-Pulseaudio

https://github.com/janvda/balena-pulseaudio/tree/master